[project]
name = "bilibili_live_tool"
version = "0.3.3"
description = "a simple tool to start or stop bilibili live stream."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "image>=1.5.33",
    "qrcode>=8.2",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.14.0",
]
